# Immutable compatibility registry fragments selected by package-set group
# snapshots. A changed fragment receives a new profile name; `default` is the
# intentionally empty profile used by snapshots needing no historical fixes.
{
  default = { };
}
